@{
    ViewData["Title"] = "Razor";
}

<div>
    <h1 class="display-4">Основы Razor</h1>
    @{
        Random rnd = new();
        int x = rnd.Next(3, 18);
        String str = "this is str";
    }
    <p>
        &commat;x = @x<br />
        &commat;str = @str<br />
        E-mail: str-length@str.Length<br />
        Generic-выражение: &commat;List&lt;int>&lt;/int> <br />
        Операции с операторами: &commat;x - 1 = @x - 1 <br/>
        Другие формы сложных выражений: 
    </p>
    <p>
        Явные выражения используют круглые скобки: &commat;(...)<br />
        str-length@str.Length &rarr; str-length@(str.Length) <br />
        &commat;x - 1 = @(x - 1) <br />
    </p>
    <p>
        Условные конструкции: &commat;if  та &commat;switch <br />

    </p>
    @if(x < 10)
    {
        int y = 20;
        <b>Этот блок будет сверстано если x &lt; 10</b>
        y -= 2;
        <i>И y будет иметь значение @y</i>
        @:Конец блока
    }
    else
    {
        int y = 30;
        <b>Этот блок будет сверстано если x &ge; 10</b>
        y += 2;
        <i>И y будет иметь значение @y</i>
        @:Конец блока
    }
    <p>
        @switch(x)
        {
            case 3 :
                <i>x = 3</i>
                break;
            case 5 :
                <i>x = 5</i>
                break;
            default:
                <i>x а не 3, а не 5</i>
                break;
        }
    </p>
    <p>
        Операторы повторения
    </p>
    <ul>
        @for(int i = 0; i < x; i++)
        {
         <li>Рядок с i = @i</li>   
        }
    </ul>
    @{
        String[] items = { "item 1", "item 2", "item 3", "item 4" };
    }
    <ol>
    @foreach(String item in items)
    {
        <li>item</li>
    }
    </ol>
</div>